{
	"extends": "./tsconfig.build.json",

	"compilerOptions": {
		"module": "ESNext",
		"declaration": true,
		// "declarationMap": true, // TODO: remove if it doesn't work

		"outDir": "dist/esm"
	},

	"references": [{ "path": "./tsconfig.package.json" }]
}
