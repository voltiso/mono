{
	"extends": "@voltiso/config.ts.eslint",

	"include": [
		"**/*.*",
		"package.json" // need to explicitly include? (if including it from code)
		//
	],

	"exclude": [
		"dist",
		"node_modules",
		".tsc-out"
		//
	]
}
