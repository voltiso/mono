import { Assert } from '../../../bdd'
import { Have_exactOptionalPropertyTypes } from '../..'

describe('Have_exactOptionalPropertyTypes', () => {
	it('works', () => {
		expect.assertions(0)
		Assert<Have_exactOptionalPropertyTypes>()
	})
})
