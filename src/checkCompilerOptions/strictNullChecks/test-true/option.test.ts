import { Assert } from '../../../bdd'
import { Have_strictNullChecks } from '../..'

describe('Have_strictNullChecks', () => {
	it('works', () => {
		expect.assertions(0)
		Assert<Have_strictNullChecks>()
	})
})
