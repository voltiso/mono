// type IsEqual = (a: unknown, b: unknown) => boolean

// export function isEqual(a: unknown, b: unknown): boolean {
// 	const res = resolve('com.voltiso.ts-util.isEqual')

// 	if (typeof Zone !== 'undefined') {
// 		const res = Zone.current.get('com.voltiso.ts-util.isEqual') as
// 			| IsEqual
// 			| undefined

// 		if (typeof res !== 'undefined') return res(a, b)
// 	}
// }

export {}
