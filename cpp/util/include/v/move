#pragma once
#include <v/_/_>

namespace VOLTISO_NAMESPACE {
template <class T>
[[nodiscard]] constexpr decltype(auto) move(T &&obj) noexcept {
	// same as std::move for now
	// return static_cast<std::remove_reference_t<T> &&>(obj);
	return std::move(obj);
}
} // namespace VOLTISO_NAMESPACE
